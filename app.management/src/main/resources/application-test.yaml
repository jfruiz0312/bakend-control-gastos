server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://dpg-d8rb26favr4c73ebek6g-a.virginia-postgres.render.com/management_db_54e1?sslmode=require}
    username: ${DB_USER:management_user}
    password: ${DB_PASSWORD:r1HQVQZYppwuaH5aYktHB60Esf63lgFJ}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  sql:
    init:
      mode: never

logging:
  level:
    com.fernandoruiz.app.management: DEBUG
    org.hibernate.SQL: DEBUG

app:
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:5173}